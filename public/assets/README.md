# Assets

Expected files:

- `public/assets/office/open-office.jpg` - the office reference/photo texture.
- `public/assets/characters/main-person.jpg` - the character photo used by all current sprites.

The game works without these files by generating temporary placeholder textures. Drop the real photos into these paths and restart the dev server to use them.
